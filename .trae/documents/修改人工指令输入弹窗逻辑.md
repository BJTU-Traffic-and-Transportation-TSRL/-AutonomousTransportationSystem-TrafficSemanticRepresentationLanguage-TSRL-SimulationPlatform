1. **修改 ManualInstructionDialog 类**：
   - 添加 mode 属性，支持 'view' 和 'input' 两种模式
   - 重构 create_widgets 方法，实现两种模式的 UI 切换
   - 添加 switch_to_input_mode 和 switch_to_view_mode 方法
   - 修改按钮事件处理逻辑
   - 移除 __init__ 中的初始暂停调用

2. **更新 run_scenario 方法**：
   - 移除初始暂停逻辑，让仿真正常启动
   - 保持弹窗显示，但初始为查看模式

3. **实现模式切换逻辑**：
   - 查看模式下，点击按钮切换到输入模式，同时暂停仿真
   - 输入模式下，点击确认或取消按钮，切换回查看模式，同时恢复仿真

4. **优化弹窗 UI**：
   - 查看模式：仅显示"进行人工交互"按钮
   - 输入模式：显示文本输入框、"确认完成输入"和"取消输入，返回"按钮

5. **确保仿真控制逻辑**：
   - 模式切换时正确发送暂停/恢复信号
   - 指令文件的读写逻辑保持不变

6. **测试验证**：
   - 确保弹窗与仿真进程通信正常
   - 验证两种模式的切换流畅性
   - 测试仿真暂停/恢复功能